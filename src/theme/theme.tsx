import { spacing } from './spacing';
import { typography } from './typography';

export const theme = {
  colors: {
    primary: '',
    background: '#000',
    text: ''
  },
  spacing,
  typography,
};